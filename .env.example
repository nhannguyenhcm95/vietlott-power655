# No credentials are required by the current sources. Never commit real secrets.
VIETLOTT_BASE_URL=https://vietlott.vn
# Fallback only; the adapter discovers the live key from the results page.
VIETLOTT_655_DRAW_KEY=d9f032a7
GITHUB_MIRROR_URL=https://raw.githubusercontent.com/vietvudanh/vietlott-data/master/data/power655.jsonl

HTTP_TIMEOUT_SECONDS=30
HTTP_MAX_RETRIES=4
HTTP_BACKOFF_BASE_SECONDS=1
HTTP_BACKOFF_MAX_SECONDS=60
# Politeness delay between requests to the same client.
HTTP_MIN_INTERVAL_SECONDS=1
