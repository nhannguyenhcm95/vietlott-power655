# M0-R — Audit of docs/SPECIFICATION.md

Auditor: methodology-auditor (Opus/high) · Date: 2026-09-24 · Verdict: REWORK

## Verified correct
- The special number is drawn from the remaining 49. In 1401 draws it never collides with a main number, so H2 (marginal uniform on 1..55) is correctly stated.
- The mean of Pearson X² for the main numbers is 49. Simulation gives 49.03 (R=3000, n=1401). Scaling by 54/49 is correct. The naive χ²(54) test has a true size of 0.0147 at α=.05. The simulated 95% critical value is 65.2, against 72.2 for naive χ²(54).
- The hypergeometric overlap mean is 36/55. The expected draw sum is 168; the exact pmf was computed by dynamic programming.
- Baseline 1 with α>0 always gives p<1, so no capping is needed.
- Split: 980/210/211 draws. Train ends at 00980 (2024-01-06). Validation is 00981–01190 (up to 2025-05-15). Test is 01191 (2025-05-17) to 01401.

## Findings (fix in SPECIFICATION.md)

1. **BLOCKER (§8): the rolling folds run into the test set.**
   - Restrict development folds to 00001–01190. That gives 13 folds from 501 to 1150, plus a partial fold 1151–1190. State whether the partial fold is kept or dropped.
   - Rolling evaluation over the test period happens only in the single final test run.
   - Hyperparameters and Baseline 2's W are either chosen nested per fold, or fixed once on the validation block. State which.
2. **MAJOR (§5 H6, AI-agent §14): H6 needs a pre-registered decision rule.**
   - Primary metric: per-draw mean log loss over the 55 cells.
   - Test: a one-sided paired draw-level block bootstrap of Δ = model − Baseline0. Reject only if the upper 95% bound is below 0.
   - Apply Holm across K finalists, with K capped in advance (one per model family).
   - State whether finalists are refit on 00001–01190.
   - Stopping rule: the test runs exactly once, and a negative result is reported.
   - Any result that beats Baseline 0 triggers a mandatory leakage audit.
3. **MAJOR (§6): no family-level multiplicity plan.**
   - Define a confirmatory family and control it with Holm at FWER 0.05:
     - H1 X²
     - H2 X²
     - H3 overlap at lags 1..10
     - H3 Ljung–Box at a fixed lag
     - H3 runs test
     - H3 permutation test
     - H4 year homogeneity
     - H4 rolling global max
     - H5, five aggregates
   - Per-number and per-lag follow-ups are interpreted only if their parent test rejects, with Holm applied within each.
   - Everything else is labelled exploratory.
4. **MAJOR (§6 H4): simulate whole draws.**
   - The mean of the null X² is 9·49 = 441, not 486. Fixed-margin simulation such as `r2dtable` is wrong here. Permute year labels over whole draws, or generate fresh 6-of-55 draws.
   - Fix the rolling window size and step in advance. State whether the envelope is pointwise or simultaneous; the global max statistic is recommended.
   - Report Cramér's V together with its null expectation, and state which N is used.
5. **MAJOR (§6 H5): KS on a discrete sum is misapplied.**
   - Use the exact pmf of the sum, with a Monte Carlo D statistic or a χ² test on pre-defined bins, each with a simulated p.
   - Define "low" as 1–27 against 28–55.
   - Pool the sparse consecutive-pair cells.
6. **MINOR.** The H2 reference is χ²(54) or simulated, and must not be scaled by 54/49. The simulated E[X²] is 54.26. An optional conditional test: the special number is uniform over the 49 numbers not drawn as main.
7. **MINOR (H3).**
   - State the overlap statistic, with a simulated p.
   - Set the runs-test tie rule; about 15 draws equal 168.
   - Fix the Ljung–Box lag set.
   - Simulated p = (1+#{T_sim ≥ T_obs})/(R+1).
8. **MINOR (§9).**
   - The bootstrap resamples whole draws. State block length, B ≥ 2000 and the seed.
   - Baseline 0 has a constant per-draw loss (log loss 0.344610, Brier 0.097190). Its CI is zero-width, and its ECE is trivially perfect.
   - Log loss is proper for the marginals only, and H6 concerns marginals.
   - Require Σp = 6, or report the actual sum.
   - Add a set-level log score for joint models (Baseline 0 = ln C(55,6) = 17.182).
   - Report MAE/RMSE with the paired bootstrap.
9. **MINOR (§7).**
   - Baseline 1: fix α (e.g. 1) or tune it on validation. It must be > 0.
   - Baseline 2: define behaviour when fewer than W draws are available.
   - Features may use all history before t. Fitted parameters may use only draws ≤ 01190.
10. **MINOR (governance).**
    - Add code_version, model_version, seed, status and limitations to the experiment record.
    - Add a spec version and changelog, and freeze the spec by hash or date before any test evaluation.
    - The live holdout is scored only with frozen finalists, has its own dataset_version, and is reported separately.
11. **Scope: no finding.** Per-number tables must never be presented as ranked "hot/cold" or "recommended" lists.

## Ruling: EDA / M4 data range
- M3 EDA may use only 00001–01190. Outcome statistics on the test period are not allowed. Label 2025 as partial.
- Full range 00001–01401 is allowed only for data-quality and structural checks that do not use number outcomes, as in M2.
- M4 confirmatory H1–H5 run on 00001–01190 now.
- M4 on the test period and the full range runs once, only after the M6 final test evaluation is locked. It is reported as a replication and never feeds back into models.
- H6 is evaluated on the test period only, once. Live draws (≥ 01402) go to the live holdout.
- Anyone who has already seen test-period outcome statistics must record it under TASKBOARD Decisions as a limitation. As of this audit, no such exposure has occurred.
